[tool.ruff]
target-version = "py37"
line-length = 88

[tool.ruff.lint]
# DTZ005: timestamp() formats a local wall-clock time for console log
# lines, not a stored/serialized value, so timezone-awareness isn't needed.
ignore = ["DTZ005"]
